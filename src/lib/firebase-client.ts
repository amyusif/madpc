// Firebase has been removed. This project now uses Neon (PostgreSQL) + Prisma.
// File storage is handled by Vercel Blob via /api/upload.
// Authentication is handled by JWT cookies via /api/auth.

export const app = null;
export const analyticsPromise = Promise.resolve(null);
export const auth = null;
export const storage = null;

